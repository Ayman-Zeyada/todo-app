import { DayHeaderPipe } from './day-header.pipe';

describe('DayHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new DayHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
